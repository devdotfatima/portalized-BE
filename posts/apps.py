from django.apps import AppConfig


class PostsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'posts'

    def ready(self):
        from .firebase import initialize_firebase  # adjust path if needed
        initialize_firebase()
